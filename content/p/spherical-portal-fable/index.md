+++
title = "Spherical scaling portals: what the simulations say"
date = 2026-07-09
description = "A companion to the video. I take each claim about spherical scaling portals — the outward self-force, the self-collision, the golden-angle fractal, the FEM gravity with its 1/r² background — pin it down with a small simulation, and then keep pulling the thread into alternative models: other rules for inertial mass, other ways to do the gravity, and what happens in 3D."

[taxonomies]
tags = ["portals", "physics", "math", "gravity", "simulation"]

[extra]
image = "sims/fig_phyllotaxis.png"
use_katex = true
hide_from_home = true
+++

{{ admonition_start(color="red", title="This article is AI generated!") }}
This article and all the code was generated by the model Claude Fable 5, given the prompt that looks like "read the script of my video and write simulation to explore everything that genuinely surprised you". And then with the prompt to explore alternative physics. And then I asked it to make it into article, refactor visualizations a little and use my own blog features to make it look good.

This is just a preview of what models think about the video, and their mathematical capabilities. IDK if this text worth reading and exploring, decide for yourself.
{{ admonition_end() }}

This is a companion to the video, for someone who has already watched it. You already know the setup: a small portal sphere sitting inside a big one makes a *pocket space* that repeats inward, toward the infinitely small, and outward, toward the infinitely large; a body colliding with its own shrunken image gets crushed; the golden‑angle arrangement grows a gorgeous fractal; and the finite‑element gravity produces an outward *self‑force* with a `1/r²` negative‑mass background.

What follows takes each of those and does two things: first checks it with a small simulation, then keeps going — *what if inertial mass changed differently on teleport? what if the FEM did gravity another way? is any of this the only consistent choice?* Everything is reproducible with nothing but `numpy` and `matplotlib`; the exact scripts are folded into the page under each result. Two folders: **`sims/`** checks the video's own claims, **`sims_alt/`** explores the alternatives.

{{ admonition_start(color="blue", title="One idea underneath everything") }}
Almost every result below is secretly a statement about a **flat torus**. The log‑polar coordinate change from the video turns the whole pocket space into one, and once you see that, the self‑force law, the uniqueness of the `1/r²` background, the equal‑mass images, and the collapse of the collision all fall out of properties of that torus.
{{ admonition_end() }}

# The pocket space is a flat torus

In the concentric arrangement the video arrives at (both spheres sharing the fixed point), one teleportation is a **similarity** of the plane:

{{ katex(body="z \;\mapsto\; \lambda\,z, \qquad \lambda = k\,e^{i\alpha}, \qquad 0 < k < 1,", block=true) }}

where {{ katex(body="k = r/R") }} is the scale ratio of the two spheres and {{ katex(body="\alpha") }} their relative rotation. The fixed point is the origin. Now take the video's own log‑polar move — {{ katex(body="u = \ln r") }} for the radius, {{ katex(body="\theta") }} for the angle — and the teleportation becomes a plain shift:

{{ katex(body="(u,\theta) \;\sim\; (u + L,\; \theta + \alpha), \qquad L = \ln(1/k).", block=true) }}

That is the definition of a **flat torus**. The angle {{ katex(body="\theta") }} already lives on a circle ({{ katex(body="\theta \sim \theta + 2\pi") }}); the log‑radius {{ katex(body="u") }} is now glued into a second circle of circumference {{ katex(body="L") }}. The two directions of the pocket space — "repeats horizontally" and "small copies run off downward" in the video — *are* the two cycles of a torus. And the portal **rotation** {{ katex(body="\alpha") }} is not a separate complication: it is just a **twist** of that torus, a parallelogram identification instead of a rectangle.

Two facts I'll lean on throughout:

* The video's remark that the non‑concentric and concentric arrangements give "exactly the same recursive space" is precisely the statement that they are **conjugate by a translation** — the one that moves the fixed point {{ katex(body="z^{*} = \dfrac{t}{1-\lambda}") }} to the origin. Same torus, different coordinates.
* In these coordinates the flat metric of the plane is {{ katex(body="ds^2 = e^{2u}\,(du^2 + d\theta^2)") }} — the torus scaled up by a conformal factor {{ katex(body="e^{2u}") }}. In **2D** the Laplacian ignores that factor (conformal invariance). That single fact is the hero — and later the villain — of the whole gravity story.

# Gravity and the self-force

## The exact law F = Gm²/2r

The video shows the self‑force as an arrow that always points away from the fixed point and "barely depends on rotation." That arrow has a closed form.

On the torus there is *no* self‑force — that was the result of the earlier torus‑gravity video, and conformal invariance carries 2D gravity onto the torus unchanged. So the push comes entirely from converting the torus gradient back into a physical force through the conformal factor. Expanding the 2D potential of a source about the body,

{{ katex(body="\ln|z - z_0| \;=\; \ln|\Delta w| \;+\; \tfrac{1}{2}\,\mathrm{Re}(\Delta w) \;+\; O(\Delta w^2), \qquad \Delta w = \Delta u + i\,\Delta\theta,", block=true) }}

the leading correction is that {{ katex(body="\tfrac{1}{2}\Delta u") }} term, and it integrates to one clean result:

{{ katex(body="F_{\text{self}} \;=\; \frac{G\,m^2}{2\,r_0}, \qquad \text{outward from the fixed point,}", block=true) }}

independent of the rotation {{ katex(body="\alpha") }}, of the scale ratio {{ katex(body="k") }}, and of the body's size.

`sims/selfforce.py` solves the Poisson problem spectrally on the (twisted) torus and integrates the real Cartesian force over the body. Here is the reconstructed field — the same "gravity steers around the images" picture from the video, because in Cartesian coordinates it does exactly that:

{{ figure_start() }}
{{ image(path="sims/fig_selfforce_a.png") }}
{{ figure_end(caption="The pocket-space gravitational field for a golden-angle twist. Blue dashed = inner portal circle, orange dashed = outer. The body is at left; its images spiral into the fixed point.") }}

Measuring the force at nine distances lands right on the predicted curve:

{{ figure_start() }}
{{ image(path="sims/fig_selfforce_b.png") }}
{{ figure_end(caption="Nine independent solver runs (dots) fall on the Gm²/2r curve (line): the self-force really does go as one over the distance.") }}

And sweeping the portal rotation across its whole range changes nothing at all — the measured‑over‑predicted ratio stays pinned at one, and the tangential component is machine zero:

{{ figure_start() }}
{{ image(path="sims/fig_selfforce_c.png") }}
{{ figure_end(caption="From 0° to 180° of relative rotation, the ratio stays at 1. Your “barely depends on rotation” is in fact “exactly independent of rotation”.") }}

Quantitatively: measured/predicted is `0.9985` at the baseline, rising to `0.9995` as the body shrinks (the residual is just finite‑body‑size {{ katex(body="O(a^2)") }} correction); the force scales exactly as {{ katex(body="m^2") }} and as {{ katex(body="1/r") }}; and it is *identical* for {{ katex(body="\alpha = 0") }}, the golden angle, and {{ katex(body="\pi") }}. So you can put a real law on screen next to the arrow, not just a direction.

{{ details_start(summary="Code — the spectral self-force solver, `sims/selfforce.py`") }}
{{ include_code(path="sims/selfforce.py", language="python") }}
{{ details_end() }}

## Why 1/r² and equal-mass images are forced

Two things the video presents as luck — "the FEM produces this out of the box," "the images have exactly the same mass, very counterintuitive" — are actually theorems.

**The `1/r²` background is the _unique_ portal‑symmetric choice.** On a compact torus the Poisson equation is solvable only if the total source is zero, so you must add a compensating negative mass — and portal symmetry forces it to be *uniform on the torus*. Uniform‑on‑the‑torus, pulled back to the plane, is exactly density {{ katex(body="\propto 1/r^2") }}. The video found the only consistent background by experiment; there was never a second option, and that is why changing the radii (which broke the naive area‑based background) leaves this one invariant.

**"All images have the same mass" is conservation of Gauss flux.** In 2D a point mass is a unit‑strength source on the torus *no matter how deep it sits*, because the conformal factor cancels between the Laplacian and the delta function. `sims/selfforce_fig.py` checks it directly: a Gauss loop around four consecutive images (radii `1.41` down to `0.18`) encloses mass `1.0000` every time. The counterintuitive fact becomes a one‑line proof — but keep the phrase "in 2D" firmly attached to it. What happens in three dimensions is the subject of a later section, and it is not this.

{{ details_start(summary="Code — field reconstruction & the equal-mass flux check, `sims/selfforce_fig.py`") }}
{{ include_code(path="sims/selfforce_fig.py", language="python") }}
{{ details_end() }}

## A caveat for the dark-energy analogy

{{ admonition_start(color="orange", title="Worth half a sentence on screen") }}
The self‑force does push everything outward forever, so the "expanding universe" mood is right. But the self‑*acceleration* is {{ katex(body="a = F/m = \dfrac{G m}{2 r}") }} — it **depends on the body's own mass** and **weakens** with distance. Real dark energy accelerates everything equally and *grows* with distance ({{ katex(body="a \propto r") }}). Qualitatively expansionary, quantitatively a different law.
{{ admonition_end() }}

## Gravity is a whole family, not a unique law

Now the first real follow‑up: *is the FEM the only way to do gravity here?* No. On the torus the source you solve against is

{{ katex(body="S \;=\; 2\pi G\,\sigma\,e^{w u},", block=true) }}

and **only {{ katex(body="w = 2") }} means "ordinary Cartesian matter"** — the case where a blob is the same Cartesian mass seen at any depth, i.e. the equal‑mass images. Every other {{ katex(body="w") }} is a perfectly self‑consistent alternative gravity, in which an image {{ katex(body="n") }} levels deep carries source {{ katex(body="k^{\,n(w-2)}") }}. Sweeping {{ katex(body="w") }} in `sims_alt/alt_gravity.py`, the self‑force strength slides smoothly through zero:

| w | 0 | 1 | **2** | 3 |
|---|---|---|---|---|
| F · r / Gm² | 0.003 | 0.179 | **0.499** | 1.055 |

{{ figure_start() }}
{{ image(path="sims_alt/fig_altgravity_a.png") }}
{{ figure_end(caption="The self-force strength is a dial. The video's ½ is simply the reading at w = 2, the point where images are equal-mass Cartesian matter.") }}

So the {{ katex(body="\tfrac12") }} is not handed down by nature — it is the {{ katex(body="w = 2") }} setting of a knob. The FEM picks it silently, because you fed it Cartesian mass.

## The battle of infinities

The same script also gives a derivation of {{ katex(body="F = Gm^2/2r") }} that may be cleaner for the video than the conformal‑factor one, because it is pure arithmetic. Sum the 2D pair force {{ katex(body="G m_1 m_2 / d") }} over all images:

1. the {{ katex(body="n") }}‑th inner image and the {{ katex(body="n") }}‑th outer image pull the body **inward** by exactly {{ katex(body="Gm^2/r") }} — for *every* {{ katex(body="n") }} and *every* rotation {{ katex(body="\alpha") }};
2. so {{ katex(body="N") }} image pairs pull inward by {{ katex(body="N \cdot Gm^2/r") }};
3. the negative haze inside the body's radius pushes it **outward** by {{ katex(body="(N + \tfrac12)\,Gm^2/r") }} when you cut both sums at the same log‑window;
4. the symmetric, zero‑monopole tie‑break is the {{ katex(body="+\tfrac12") }}. Net: {{ katex(body="\tfrac12 \cdot Gm^2/r") }}, outward. ∎

{{ figure_start() }}
{{ image(path="sims_alt/fig_altgravity_b.png") }}
{{ figure_end(caption="Images pull inward (orange), the haze pushes outward (green), and their sum (blue) is exactly ½·Gm²/r for every truncation N — and the open squares show it is unchanged by a golden-angle rotation.") }}

Verified to `0.500000000000` at {{ katex(body="\alpha = 0") }} and at the golden angle. It is "a tug of war between two infinite pulls, and the referee's coin lands on one half."

## Two invisible dials the FEM sets to zero

There is a freedom hiding in the solver worth a "here's what we're *not* showing you" beat. On the torus there are exactly two source‑free, portal‑symmetric fields (harmonic 1‑forms) that any solution can carry without changing a single mass: a **radial {{ katex(body="1/r") }} field** — mathematically an invisible point mass sitting *at* the fixed point, outside the pocket space itself — and an **azimuthal {{ katex(body="1/r") }} vortex**, as if the fixed point "spins." Neither is detectable by any local mass measurement, yet both bend orbits:

{{ figure_start() }}
{{ image(path="sims_alt/fig_altgravity_c.png") }}
{{ figure_end(caption="Same matter, different sky. The blue rosette is the plain central-mass orbit; adding a whisper of vortex (which does net work every lap) winds the same orbit into the fixed point.") }}

Your FEM quietly sets both dials to zero.

{{ details_start(summary="Code — the family of gravities, the four-line proof, the vacuum orbits, `sims_alt/alt_gravity.py`") }}
{{ include_code(path="sims_alt/alt_gravity.py", language="python") }}
{{ details_end() }}

## In 3D the whole story inverts

This is the result I least expected, and the one to keep in your back pocket for a possible 3D sequel. **Equal‑mass images, the clean {{ katex(body="Gm^2/2r") }} law, and rotation‑independence are all 2D‑only accidents of conformal invariance.** In three dimensions the same equation that gave them to you takes them away.

Pull a point mass through one portal step. The Laplacian picks up {{ katex(body="k^2") }}; the delta‑function source picks up the volume Jacobian {{ katex(body="k^{-d}") }}. So the image one level deeper carries mass

{{ katex(body="m \cdot k^{\,d-2}.", block=true) }}

That single exponent runs everything:

* **{{ katex(body="d = 2") }}:** {{ katex(body="k^0 = 1") }} → every image equal (the video's result);
* **{{ katex(body="d = 3") }}:** {{ katex(body="k^1 = k") }} → images shrink geometrically inward.

`sims_alt/three_d.py` confirms the 3D case with an actual Gauss‑flux measurement: the enclosed image masses come out {{ katex(body="1,\,k,\,k^2,\,k^3,\dots") }} to three digits.

{{ figure_start() }}
{{ image(path="sims_alt/fig_3d_a.png") }}
{{ figure_end(caption="Image mass versus depth. In 2D every image weighs the same (flat blue line); in 3D each level is a factor k lighter. The exponent is k to the power d−2.") }}

And the downstream physics inverts. With the forced {{ katex(body="k^n") }} masses, at {{ katex(body="\alpha = 0") }} the {{ katex(body="n") }}‑th inner and {{ katex(body="n") }}‑th outer images cancel *exactly* — the self‑force is zero to machine precision — and turning the portals on turns on an **inward, swirling** pull, the opposite character to 2D's rotation‑independent outward push:

{{ figure_start() }}
{{ image(path="sims_alt/fig_3d_b.png") }}
{{ figure_end(caption="3D with the forced kⁿ masses: no self-force at all when the portals are aligned; rotation generates an inward radial pull plus a tangential swirl.") }}

If instead you insist on equal‑mass copies plus a {{ katex(body="1/r^3") }} haze — the literal translation of the video's 2D recipe — images and haze both diverge, and the finite remainder even flips **sign**:

{{ figure_start() }}
{{ image(path="sims_alt/fig_3d_c.png") }}
{{ figure_end(caption="3D with equal-mass copies + a 1/r³ haze: the self-force is outward for small portals and inward for large ones, changing sign near k* ≈ 0.17.") }}

{{ admonition_start(color="violet", title="A sentence for the sequel") }}
"In three dimensions the sign of gravity depends on the size of the portal." It
is startling, and it is correct — with one honest asterisk: the 3D global
completion (the haze) is genuinely delicate, and the exact number
{{ katex(body="k^{*} \approx 0.17") }} depends on the regularization scheme. Trust
it as *a phenomenon*, not yet as *a measurement*.
{{ admonition_end() }}

{{ details_start(summary="Code — the 3D forcing law, Gauss-flux check, and both models, `sims_alt/three_d.py`") }}
{{ include_code(path="sims_alt/three_d.py", language="python") }}
{{ details_end() }}

# Collisions and inertia

The video flags the collision as "preliminary… take it with a grain of salt," because in the engine the inertial mass doesn't rescale on teleport. The good news: the salt is unnecessary. The squash is real physics of the space, not an artifact of that choice.

## The squash is universal

Unroll the recursive collision into a 1D chain of copies — copy {{ katex(body="n") }} at {{ katex(body="x_n = \xi_n k^n") }}, half‑width {{ katex(body="w k^n") }}, velocity {{ katex(body="-v k^n") }} — the body flying straight into its own smaller image. The crucial modeling point is the boundary. The engine sees a *truncated* stack, whose innermost copy has vacuum beneath it, and that alone forces a collapse. So `sims/collision.py` instead uses the **true portal identification**: the chain is scale‑periodic, its ghost copies are wrapped, rescaled versions of the real ones. No fake ends.

With that fixed, a static force balance gives a sharp criterion:

{{ admonition_start(color="green2", title="Bounce criterion") }}
In a self‑similar contact the net force on every copy points **inward** (deeper squash) unless contact stiffness grows toward smaller copies **faster than** {{ katex(body="1/k") }} — that is, unless {{ katex(body="\kappa_n / \kappa_{n-1} > 1/k") }}. No real material does that.
{{ admonition_end() }}

The simulation bears it out across three materials. In the engine model every copy's world‑line dives into the fixed point together, in finite time:

{{ figure_start() }}
{{ image(path="sims/fig_collision_a.png") }}
{{ figure_end(caption="Engine model: the world-lines of all copies collapse onto the fixed point simultaneously, in finite time.") }}

Tracking how squashed the middle copy gets, the engine and the fully scale‑consistent material (mass ∝ area, stiffness scaled the same way) both blow past 100% — fully crushed — while only the unphysical "magic" material stays near zero:

{{ figure_start() }}
{{ image(path="sims/fig_collision_b.png") }}
{{ figure_end(caption="Overlap of the middle copy over time. Engine and scale-consistent both exceed 100%; the squash is therefore NOT an artifact of the frozen-inertia model. Only the magic material barely dents.") }}

{{ figure_start() }}
{{ image(path="sims/fig_collision_c.png") }}
{{ figure_end(caption="Position of the middle copy: collapse to the fixed point for engine and scale-consistent materials, a single clean bounce for the magic one.") }}

The picture is that you are pressing on *yourself* through the portal, and your bigger self always leans on your back harder than your smaller self can push back on your front. The collapse is a true **finite‑time singularity** — infinitely many laps through the portal in finite time — which is exactly why no engine survives it.

{{ details_start(summary="Code — the self-collision chain with the true portal boundary, `sims/collision.py`") }}
{{ include_code(path="sims/collision.py", language="python") }}
{{ details_end() }}

## No inertial-mass rule saves you

Now the headline question: *what if inertial mass changed differently between teleports?* Make it a free knob. In `sims_alt/chainlib.py` copy {{ katex(body="n") }} is given mass {{ katex(body="\propto k^{\gamma n}") }} and contact stiffness {{ katex(body="\propto k^{s n}") }}, so {{ katex(body="\gamma = 0") }} is the engine, {{ katex(body="\gamma = 2") }} is scale‑consistent, {{ katex(body="\gamma = -2") }} conserves kinetic energy, and {{ katex(body="s < -1") }} is the "magic" material. Sweeping the whole {{ katex(body="(\gamma, s)") }} plane:

{{ figure_start() }}
{{ image(path="sims_alt/fig_phase_a.png") }}
{{ figure_end(caption="Worst squash over the whole plane of rules. The collapse/bounce boundary is a perfectly vertical line at s = −1: moving up or down — changing the inertial-mass rule — never crosses it.") }}

{{ figure_start() }}
{{ image(path="sims_alt/fig_phase_b.png") }}
{{ figure_end(caption="The same data as line cuts. All five mass rules give the same curve; the squash blows up exactly at s = −1, and nowhere else.") }}

So the grain of salt can come off. Survival depends **only on the stiffness rule**; whatever the right theory of inertial mass turns out to be, it does not change whether the object is crushed.

{{ details_start(summary="Code — the generalized collision chain, `sims_alt/chainlib.py`") }}
{{ include_code(path="sims_alt/chainlib.py", language="python") }}
{{ details_end() }}

{{ details_start(summary="Code — the (γ, s) phase-diagram sweep, `sims_alt/phase_diagram.py`") }}
{{ include_code(path="sims_alt/phase_diagram.py", language="python") }}
{{ details_end() }}

## Why: the twin test and the detectable seam

Why is the mass rule powerless? Because the deepest issue isn't inertia — it's whether the pocket space's dynamics is even well‑defined at a crossing. `sims_alt/seam_twins.py` makes it visible.

A trajectory {{ katex(body="z(t)") }} and its portal image {{ katex(body="\lambda\,z(t)") }} are the **same physical motion**. Throw a ball; also watch the same throw one portal deeper. If the laws evolve the two copies differently, then physics can tell which copy is "the real one" — the portal **seam becomes physically detectable**, and the quotient is inconsistent.

Under ordinary Newtonian inertia ({{ katex(body="m\ddot z = F") }}), the twins diverge — and fast:

{{ figure_start() }}
{{ image(path="sims_alt/fig_twins_a.png") }}
{{ figure_end(caption="Newtonian inertia: the throw evolved by physics (blue) peels away from the very same throw mapped through the portal (black). The seam is detectable.") }}

No inertial‑mass rule can repair this, because a **test particle carries no mass to rescale** in the first place. But if you measure velocity in units of your own *current* size — scale‑relative dynamics, {{ katex(body="\eta = \dot z / z") }} — the twins coincide to floating‑point precision, forever:

{{ figure_start() }}
{{ image(path="sims_alt/fig_twins_b.png") }}
{{ figure_end(caption="Scale-relative inertia: the twin evolved by physics (dashed) never leaves the mapped original (solid). The seam is invisible.") }}

{{ figure_start() }}
{{ image(path="sims_alt/fig_twins_c.png") }}
{{ figure_end(caption="Their separation over time. Newtonian twins reach full divergence in a fraction of a second; scale-relative twins sit at 10⁻¹² — machine noise.") }}

And here is the sting: **the scale‑relative rule is exactly the rule that has no self‑force.** The bookkeeping makes it airtight. One inward crossing forces {{ katex(body="v \to k v") }} (continuity of the shrunk copy); what that does to a ball, per rule (with {{ katex(body="k = \tfrac12") }}):

| rule for m′ | kinetic energy | momentum | grav. mass |
|---|---|---|---|
| engine: m′ = m | ×0.25 | ×0.50 | ×1 |
| scale‑consistent: m′ = k²m | ×0.06 | ×0.13 | ×0.25 |
| momentum‑conserving: m′ = m/k | ×0.50 | ×1 | ×2 |
| energy‑conserving: m′ = m/k² | ×1 | ×2 | ×4 |

Conserving energy **and** momentum needs {{ katex(body="\chi k^2 = \chi k = 1") }}, impossible for {{ katex(body="k < 1") }}; and 2D gravity separately demands {{ katex(body="\chi = 1") }} (equal‑mass images). Three irreconcilable demands.

{{ admonition_start(color="blue2", title="The genuine either/or") }}
Keep ordinary inertia → a detectable seam, a self‑force, and an unavoidable crush (**the video's world**). Go scale‑relative → seamless, force‑free, no crush — but you have changed what "velocity" means. You cannot have neither.
{{ admonition_end() }}

{{ details_start(summary="Code — the twin/seam test and the conservation table, `sims_alt/seam_twins.py`") }}
{{ include_code(path="sims_alt/seam_twins.py", language="python") }}
{{ details_end() }}

## Where the crushing energy comes from

The crush needs energy. `sims_alt/energy_ledger.py` traces it to the same Zeno structure. Take a ball coasting with **no forces at all**, straight toward the fixed point. In the unrolled plane this is nothing — uniform motion reaching the fixed point at {{ katex(body="t_c = r_0/v") }}. But the pocket‑space representative (the "copy at your scale" the engine actually stores) gains a fixed kinetic‑energy factor **every lap**, and the laps pile up Zeno‑style — infinitely many before {{ katex(body="t_c") }}. Under the engine rule the ledger reaches infinity in finite time; only {{ katex(body="m' = m/k^2") }} keeps it flat (and that rule, again, breaks momentum and gravity):

{{ figure_start() }}
{{ image(path="sims_alt/fig_ledger_a.png") }}
{{ figure_end(caption="A force-free coasting ball's ledger kinetic energy. Under the engine and scale-consistent rules it runs to infinity before the ball even reaches the fixed point.") }}

That phantom energy is what powers the collision — total kinetic energy of the colliding copies runs away by three to four orders of magnitude in the collapsing models, and stays flat in the bouncing one:

{{ figure_start() }}
{{ image(path="sims_alt/fig_ledger_b.png") }}
{{ figure_end(caption="Total energy of the simulated copies during the crush: ×1000–8000 for the collapsing materials, ×1.00 for the bounce, which returns its energy honestly.") }}

So "everything shatters to pieces" isn't the engine giving up — it's a real finite‑time energy runaway baked into the geometry of "your own scale."

{{ details_start(summary="Code — the energy ledger, `sims_alt/energy_ledger.py`") }}
{{ include_code(path="sims_alt/energy_ledger.py", language="python") }}
{{ details_end() }}

# The golden-angle scene, rebuilt from the numbers

The video renders the golden‑angle fractal with "an alternative method based on coordinate systems" and leaves the details for another time. `sims/phyllo.py` rebuilds it straight from the stated numbers — outer sphere {{ katex(body="R = 141\text{ km}") }}, inner {{ katex(body="r = 140\text{ km}") }}, a {{ katex(body="4\text{ m}") }} gap, golden‑angle twist. The torus makes it a two‑line calculation: the teleportation shrinks by {{ katex(body="k = 140/141 = 0.992908") }} and rotates by {{ katex(body="137.5^\circ") }}.

{{ figure_start() }}
{{ image(path="sims/fig_phyllotaxis_a.png") }}
{{ figure_end(caption="All 2600 images spiralling into the fixed point; marker size is proportional to image scale (exaggerated). The whole fractal lives hundreds of times closer to the fixed point than the portals are wide.") }}

{{ figure_start() }}
{{ image(path="sims/fig_phyllotaxis_b.png") }}
{{ figure_end(caption="The same images in log-polar coordinates: the spiral becomes a perfectly even lattice. This is the video's “geometric progression → arithmetic progression” animation, frozen into one still.") }}

Three facts drop out:

{{ pros_start(text="From the scene's own numbers:") }}
* **The fixed point is tiny and close.** Taking "4 m" as the surface gap, it sits just **536 m from the sphere centers** — the entire fractal lives in a region hundreds of times smaller than the 140 km portals. (Read as centre‑to‑centre distance instead, it's **2.15 m**. Either way, microscopic next to the spheres.)
* **Depth.** It takes **1665 images** to shrink from the object to 1 m, and **2636** to reach 1 mm.
* **Fibonacci isn't decoration — it's forced.** Ranking image‑lattice neighbours by distance in the log‑polar metric, the closest sit at steps {{ katex(body="n \pm \{8, 13, 21, 34\}") }} — pure Fibonacci, the same parastichy families a sunflower shows, from the same golden‑angle lattice.
{{ pros_end() }}

{{ details_start(summary="Code — the golden-angle scene, `sims/phyllo.py`") }}
{{ include_code(path="sims/phyllo.py", language="python") }}
{{ details_end() }}

# Why there is no double pocket space

The video states, almost in passing, that two pairs of spherical portals can't be nested to make a *double* pocket space. That is provable, and the proof is short.

A portal teleportation is always a **similarity** of the plane (scale, rotate, translate). A group of similarities that acts *properly discontinuously* — the condition for a clean, non‑overlapping recursive space — must be **virtually cyclic** (essentially a single repeating direction). Sketch: two contractions with *different* fixed points generate, by conjugating one with high powers of the other, group elements arbitrarily close to the identity — which destroys discreteness. The multi‑handle "double pocket" spaces you might hope for (Schottky‑type) need circle **inversions**, and no flat scaling portal can produce an inversion.

So a single pocket is all this mechanism can *ever* give. It isn't a limitation of the search — it's a theorem about similarity groups.

# For the script: what is safe to say, and what to hedge

{{ pros_start(text="Safe to state plainly:") }}
* The self‑force is a **law**, not just an arrow: {{ katex(body="F = Gm^2/2r") }}, outward, exactly independent of rotation, scale ratio, and body size. Put the five symbols next to the FEM arrow.
* The {{ katex(body="1/r^2") }} background and the equal‑mass images aren't luck — they're the **unique** portal‑symmetric choice and a conservation law.
* The collision is **unavoidable**: no rule for inertial mass changes the verdict; the object collapses into the fixed point in finite time, and that is a property of the space.
* The golden‑angle scene's Fibonacci arms (8, 13, 21, 34) come straight out of the numbers, and the fixed point is about 500 m from the centres of 140 km spheres.
* **One pocket** is all you can get — provably.
{{ pros_end() }}

{{ cons_start(text="Hedge, each in half a sentence:") }}
* The dark‑energy analogy is qualitative; {{ katex(body="a = Gm/2r") }} is not {{ katex(body="a \propto r") }}.
* Equal‑mass images and the clean {{ katex(body="Gm^2/2r") }} are **2D miracles**; 3D inverts both the sign and the character. Don't let a viewer assume they carry over.
* The FEM's {{ katex(body="\tfrac12") }} is a **choice** ({{ katex(body="w = 2") }}), not the only consistent gravity.
* The 3D equal‑copy "haze" is genuinely delicate; the sign‑flip {{ katex(body="k^{*} \approx 0.17") }} depends on the regularization. A phenomenon, not yet a measurement.
{{ cons_end() }}

# Running the code

Everything runs with just `numpy` and `matplotlib`. Each script prints its numbers and writes its figures next to itself:

{{ details_start(summary="Commands to regenerate every figure") }}
```bash
# the video's own claims (sims/)
python sims/selfforce.py          # the F = Gm²/2r law, all the sweeps (text)
python sims/selfforce_fig.py      # → fig_selfforce*.png  (+ Gauss-flux equal-mass check)
python sims/collision.py          # the 1D self-collision chain (text verdicts)
python sims/collision_fig.py      # → fig_collision*.png
python sims/phyllo.py             # → fig_phyllotaxis*.png (fixed point, Fibonacci)

# the alternatives (sims_alt/)
python sims_alt/phase_diagram.py  # sweep (γ, s) → phase.npz
python sims_alt/phase_fig.py      # → fig_phase*.png
python sims_alt/seam_twins.py     # → fig_twins*.png       (twin/seam test + table)
python sims_alt/energy_ledger.py  # → fig_ledger*.png
python sims_alt/alt_gravity.py    # → fig_altgravity*.png  (matter dial, 4-line proof, vacuum)
python sims_alt/three_d.py        # → fig_3d*.png          (3D forcing law, sign flip)
```
{{ details_end() }}

The reusable core is small: `sims/selfforce.py` (the spectral Poisson solve on the twisted torus) and `sims_alt/chainlib.py` (the generalized collision chain); `vizstyle.py` just holds the shared palette and the panel‑splitting helper.

| Claim in the video | Where it's tested | Figures |
|---|---|---|
| Outward self‑force, "barely depends on rotation" | `sims/selfforce.py` | `fig_selfforce_a…c` |
| Large/small images have equal mass | `sims/selfforce_fig.py` | Gauss‑flux check |
| `1/r²` negative mass is the right background | uniqueness argument | — |
| Head‑on collision shatters everything | `sims/collision.py` | `fig_collision_a…c` |
| Golden‑angle fractal, Fibonacci, fixed point | `sims/phyllo.py` | `fig_phyllotaxis_a,b` |
| No double pocket space | similarity‑group theorem | — |
| *What if inertial mass rescaled differently?* | `sims_alt/phase_diagram.py`, `seam_twins.py`, `energy_ledger.py` | `fig_phase`, `fig_twins`, `fig_ledger` |
| *What if the FEM did gravity differently / is it unique?* | `sims_alt/alt_gravity.py`, `three_d.py` | `fig_altgravity`, `fig_3d` |
